/**
 * Gameunity — sidebar.js (STUB)
 *
 * M2 FIX: All sidebar logic has been merged into app-rail.js.
 * This file is kept as a no-op stub so that any page still importing
 * sidebar.js explicitly does not get a 404.
 *
 * You may safely delete this file once you've confirmed all pages
 * import app-rail.js and no page still has:
 *   <script src="../js/components/sidebar.js"></script>
 * as its ONLY sidebar script.
 *
 * Functions now in app-rail.js:
 *   - window.toggleSubSidebar()
 *   - window.toggleModSidebar()
 *   - window.toggleSection(sectionId)
 *   - initTooltips()
 */
console.debug('[sidebar.js] STUB — logic lives in app-rail.js');
